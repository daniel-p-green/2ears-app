import SwiftUI

/// The app's colors, all system colors. The accent (cyan) is the asset-catalog AccentColor.
enum Theme {
    /// Deep blue behind navigation titles, in the Sleep app's idiom.
    static let glow = Color(red: 0.06, green: 0.20, blue: 0.52)
    /// Over the threshold: the one warning color.
    static let over = Color.orange
}
